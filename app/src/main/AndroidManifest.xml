<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.longluo.ebookreader">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".app.AppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.tab_bar.activity.TabBarActivity"
            android:label="@string/app_name"
            android:launchMode="singleInstance"
            android:theme="@style/Theme.EbookReader.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.main.DemoActivity" />

 <!--       <activity android:name=".ui.tab_bar.activity.TabBarActivity" />-->

        <activity android:name=".ui.viewpager.activity.ViewPagerActivity" />


        <activity android:name=".EpubActivity" />

        <activity
            android:name=".ChapterDetailActivity"
            android:launchMode="singleInstance" />

        <activity android:name=".FileListActivity" />

        <activity android:name="com.longluo.android.pdfviewer.pdfui.DocumentActivity" />
        <activity android:name="com.longluo.android.pdfviewer.pdfui.OutlineActivity" />

    </application>

</manifest>